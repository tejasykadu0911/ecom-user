#spring.application.name=user


spring:
  application:
    name: user-service

  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

server:
  port: 8082